# DevPro MVP - Backend (TypeScript + Express + SQLite)

## Run locally

1. `cd backend`
2. `npm install`
3. `npm run build`
4. `npm start`

For development use `npm run dev` (requires ts-node-dev).

The app will create the SQLite DB file at `dist/../data/tasks.db` on first run.
