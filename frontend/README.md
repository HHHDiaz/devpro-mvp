# DevPro MVP - Frontend (TypeScript -> minimal browser modules)

This is a minimal frontend using TypeScript sources in `src/` and a prebuilt `dist/` for convenience.
To run the full app, serve `backend/public` via the backend server (it already includes the built JS).
